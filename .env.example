# Postgres（Vercel 生产必填；Neon / Vercel Postgres 连接串）
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require

# 可选：覆盖 HMAC 登录签名密钥
# TOKEN_SECRET=change-me

# 本地开发可不用 DATABASE_URL，将使用项目目录 data.json / config.json
