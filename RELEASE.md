# RELEASE

| 版本 | 日期 | 说明 |
|------|------|------|
| 4.1.7 | 2026-07-27 | 飞书卡「已收到」HMAC 深链；待办确认后归档；可撤销今日确认 |
| 4.1.5 | 2026-07-27 | VPS 推送即发布：GitHub Actions → `49.235.172.214:3333`（SSH 2222 + PM2） |
| 4.1.4 | 2026-07-27 | 飞书改用官方长连接 `npm run feishu:ws`（绕过 Vercel 3s 超时） |
| 4.1.2 | 2026-07-27 | 今日页体验：进度条、同名合并、可关闭提示、骨架屏 |
| 4.1.1 | 2026-07-27 | 飞书应用机器人事件入口（3s 校验）；网页去掉确认/助手；space 清单 |
| 4.0.1 | 2026-07-27 | Nudge 品牌与调度补强 |

## 4.1.7 迭代内容

- **确认**：事项推送卡带签名「已收到」按钮/链接；`/api/ack` 无 `sig` 拒绝（网页不做「确认收到」主按钮）
- **待办**：确认后 `archived`+停用；清单「已归档」区可取消归档
- **撤销**：今日已确认列表与详情可「撤销今日确认」
- **调度**：VPS `reminder-check` 每 5 分钟（对齐 HH:mm）

## 4.1.1 迭代内容

- **飞书**：轻量 `/api/feishu/event`（URL challenge &lt; 3s）；回复「收到」确认今日事项；其它消息走 DeepSeek
- **前端**：今日只读预览；移除助手 Tab 与网页「确认收到」；清单按习惯/日子/待办
- **推送**：事项卡与热点卡分通道
- **部署**：`vercel.json` 单独打包飞书事件函数；需配置 `FEISHU_APP_ID` / `FEISHU_APP_SECRET`
